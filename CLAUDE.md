# CLAUDE.md

## プロジェクト概要
ループエンジニアリングの playbook、人が毎回レビューしなくても信用できる「AIにコードを書かせ続けるループ」を1本 実際に作り、本物の OSS リポジトリまで回して実証したもの

仕組みとセットアップは [README.md](README.md)、設計の全部は [DESIGN.md](DESIGN.md)

## コーディング規約（harness/）

**ハーネスを変えたら [MAINTAINING.md](MAINTAINING.md) の回帰（迂回＋並列＋正常系）を回してから戻す**、ワーカー1本の検証だけで済ませると並列でしか出ない壊れ方を見逃す

- ハーネスは素の Node（ESM・`.mjs`）で書く、重い依存や SDK は足さない（`claude` / `bd` / `git` / `npm` / `npx` は子プロセスで呼ぶ）
- ハーネスが唯一の門番、ワーカー（`claude`）に commit / close をさせない ── **権限では止まらない**（作業コピーの中で `git` も `bd` も叩ける）ので、呼び出しの前後で検出して取り消す・開け直す
- 破壊的な git 操作は**対象を必ず明示する**、`-A` や無指定で全体に効かせない
  - 書き直しのたびの巻き戻しは**パスで限定**（`git checkout/clean -- src tests`）、作業領域の外を戻す時も**戻すファイルを列挙して**渡す
  - HEAD やブランチを動かす操作（`reset --mixed/--hard`・`checkout -B`）は**自分が直前に取った SHA へ戻す時だけ**（例: ワーカーのコミットの取り消し・統合が赤だった時の巻き戻し）
- **ゲートの判定材料をワーカーに触らせない** ── テスト・設定（`stryker.config.json`）・テストコマンド・変異チェックの無視の印・タスクの状態、どれも**権限では守れない**ので呼び出しの前後で検出して戻す（迂回の再検証は [proof/stub-worker-attack.mjs](proof/stub-worker-attack.mjs)）
- **共有している状態の変化を、目の前のワーカーのせいと決めつけない** ── タスクリストは全ワーカーで共有、並列では別のワーカーの正しい完了が混ざる（実際に起きた: 互いの完了を取り消し合って同じタスクを延々とやり直した）
- 成否は exit code で判定（パイプ越しの `$?` は信用しない）
- 子プロセスへ渡すパスは絶対パスに正規化する（Windows のドライブレターは大文字へ）、相対パスのままだと起動シェルの表記が伝わって環境依存の事故になる
- 受け取った値を勝手に読み替えない、**解決した結果を見せて 前提を満たさなければ止める**（例: `BASE` を表示して git リポジトリでなければ exit 1）、黙って別の場所を触ると人が気づけない
- 不合格にする時は理由の1行だけでなく中身を残す（判定に使った出力・巻き戻す前の成果物）、残さないと人が原因を調べられない
- 用途を広げる時はハーネスを複製せず拡張点を足す（現在 `WORK_ROOT`・`MUTATOR`）、複製は本体の修正に追従せず必ず腐る
- 拡張点は実走で必要と分かったものだけ足す、想像で汎用化しない

## 実走（実験）の進め方

**[.claude/rules/experiment-hygiene.md](.claude/rules/experiment-hygiene.md)** ── リポジトリの外でやる／最新をベースにする／確証を得たものだけ反映する／作業ツリーへ破壊的な git 操作を打つ前に確認を取る／`claude` の課金

コマンドを打つ時に効くルールなので、対象ファイルを絞らず毎セッション読まれる

## コミット規約
- **コミットの前に著者が個人の identity か確認する**（`git config --local user.name`）── global は仕事用なので、公開リポジトリでは local に個人のものを設定する
- コミットメッセージは日本語
- コミットメッセージに著者表記（`Co-Authored-By` など Claude/Anthropic の表記）を絶対に入れない

## ドキュメント規約
- **絵文字と評価の記号を使わない**、良し悪しは言葉で書く（あり／無し／間接的）── 掛け算の `×` は例外
- **行末**に句点（。）を付けない、体言止めにするか 読点でつないで短く終える（**文中で文を区切る句点は残す**、読点でつなぐと悪文になる）
- 読者向けの文書に内輪の略語・比喩を使わない（リポ→リポジトリ、棚→タスク 等）、用語は初出で定義する
- **書き手の頭にある専門用語をそのまま出さない** ── 「契約」「インターフェース」「スキーマ」のような設計の言葉は、読者には**何を書けばいいか**に翻訳する（契約→**返す値の形**、インターフェース→**関数の書き方**）
  - 実際に起きた: 変異器の作り方を「契約」と書いた、読者は「何を守れば動くのか」が分からない
  - 判定: **その語を消しても手順が書けるなら消す**、消せないなら初出で1行 定義する
- 実証していない主張を書かない、削る時は読者が次に何をすればいいかへ着地させる
- 書き方の詳しい基準は [.claude/rules/](.claude/rules/) にある（対象のファイルを読むと自動で読み込まれる）
  - [doc-guidelines.md](.claude/rules/doc-guidelines.md) 保守するドキュメント共通 ／ [readme-guidelines.md](.claude/rules/readme-guidelines.md) README.md ／ [sample-readme-guidelines.md](.claude/rules/sample-readme-guidelines.md) sample の README
  - **レビューする時の観点は [doc-review-passes.md](.claude/rules/doc-review-passes.md)** ── この順で全部 通す、観点を減らさない

## 保守の手順

**こうしたとき何を確認するか**は [MAINTAINING.md](MAINTAINING.md)、このファイルと違って自動では読まれないので**次のどれかをやる時に開く**

- **ドキュメントを直した** → 検査を走らせる → 中身は `/review-<対象>`
- **ハーネスを直した** → 検査 → 回帰（迂回＋並列＋正常系） → 規約と断定の照合
- **検査（`inspect.mjs`）を足した** → わざと違反を入れて落ちることを確かめる（複製先で・このリポジトリを汚さない）
- **実験する（実走・課金あり）** → [実験の作法](.claude/rules/experiment-hygiene.md)
- **GitHub に公開する** → 公開する前に

## ドキュメント体制（SoT）

**1つの情報は1箇所だけ**、同じ事実を複数ファイルに書かず SoT（どのファイルが真実か）を決めてリンク参照する

| 情報 | SoT |
|------|-----|
| 仕組み・セットアップ・使い方・実証の記録・既知の限界 | [README.md](README.md) |
| 設計の全部（通しフロー・全決定・なぜ各ゲートが要るか） | [DESIGN.md](DESIGN.md) |
| 検証（各段階が何を証明したか・実測・落とし穴） | [proof/README.md](proof/README.md) |
| ハーネス本体（採り入れる道具） | [harness/](harness/) |
| 実際に回した題材一式（設定・タスク文・生成された成果物とテスト・実測） | [sample/](sample/) |
| コーディング/コミット/ドキュメント規約 | この CLAUDE.md |
| 保守の手順（こうしたとき何を確認するか） | [MAINTAINING.md](MAINTAINING.md) |
| 実験の作法（毎セッション読まれる） | [.claude/rules/experiment-hygiene.md](.claude/rules/experiment-hygiene.md) |
| レビューの手順（対象ごと・`/review-…` で呼ぶ） | [.claude/skills/](.claude/skills/) |
| 書き方の詳しい基準（対象ごと・自動で読み込まれる） | [.claude/rules/](.claude/rules/) |
| 進捗・内部メタ（リポジトリ情報・未決定・現在の焦点） | WIP.md（git 管理外・公開リポジトリには含めない） |

## 作業状況（WIP）

進行中の作業メモ（進捗・優先順位・未決定・リポジトリ情報・現在の焦点）は `WIP.md`（git 管理外の個人メモ・公開リポジトリには含めない）に置く

- **セッション開始時**に `WIP.md` を読んで前回の状況・今の焦点を把握、必要なら参照先（例: 設計の再検討中なら `DESIGN.md`）へ辿る
- **作業の区切り**で `WIP.md` を最新化（完了へ移動・却下・次の一手・未決定を反映）
